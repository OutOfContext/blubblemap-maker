# Keine echten Zugangsdaten in eine versionierte `.env`-Datei eintragen.
# Verwende stattdessen eine lokale, nicht versionierte `.env` oder dokumentiere
# die benötigten Variablen in einer separaten `.env.example`.
# Benötigte Variable:
# OPENAI_API_KEY=<your-openai-api-key>